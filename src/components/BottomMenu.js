const BottomMenu = () => {
    let menuitems = { };
    
    
    return (
        <div className="BottomMenu">
            <ul className="menu-items">
                {{ menuitems }}
            </ul>
        </div>
    );
}

export default BottomMenu;